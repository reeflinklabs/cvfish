{
  "dependencies": {
    "autoprefixer": "^10.4.19",
    "daisyui": "^4.12.10",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.4"
  },
  "scripts": {
    "build:css": "postcss styles.css -o output.css",
    "dev": "npm run build:css && live-server --port=3000 --watch=./ --open=index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
